class UiController < ApplicationController

  # # one way redirect all requests to index.html page (it uses in case of external-rails branch where rails-server and client are working in one place)
  # def index
  #  redirect_to "/client/index.html", :status => :moved_permanently
  # end

  # another way to redirect we can setup in routes.rb
end
